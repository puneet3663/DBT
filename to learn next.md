What you can explore next:

Snapshots — track slowly changing data over time
Incremental models — only process new rows instead of full refresh
dbt macros — more reusable SQL logic
Multiple sources — add more CSV files or connect to real databases
Scheduling — automate dbt build using Windows Task Scheduler

